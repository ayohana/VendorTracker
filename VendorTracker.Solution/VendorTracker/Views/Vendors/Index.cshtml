@using VendorTracker.Models;
@{
  Layout = "_Layout";
}

@if (@Model.Count == 0)
{
  <h4>There are currently no vendors saved.</h4>
}
else
{
  <ul id="vendorList">
  @foreach(Vendor vendor in Model)
  {
    <li><a href="/vendors/@vendor.ID">@vendor.Name</a></li>
  }
  </ul>
}